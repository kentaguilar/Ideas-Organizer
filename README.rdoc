A prototype version of a functional Ideas Organizer.
The app also has a security feature(login/logout mechanism) built via Devise.

The project is still in-progress and will be having loads of updates soon.